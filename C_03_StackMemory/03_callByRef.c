/*#include <stdio.h>
#include <stdlib.h>

struct node*{
    int data;
    struct node *top;
};

struct node* pop(struct node* top){
    struct node *temp;
    temp = top;
    return temp;
}

int node* pop(struct node **top){

    int data = *top->data;
    struct node *temp;
    temp = *top;
    *top = *top->link;
    free(temp);
    temp = NULL;
    return data;
}

int main(){

}*/